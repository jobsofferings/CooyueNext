interface:
  display_name: "Cooyue DB"
  short_description: "Inspect CooyueNext DB config and counts"
  default_prompt: "Use $cooyue-db to inspect CooyueNext PostgreSQL health, row counts, and server API files."
