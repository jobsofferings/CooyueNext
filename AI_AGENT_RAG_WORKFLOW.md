# AI Agent 与 RAG 融合学习路线整理

来源：[Doubao 线程](https://www.doubao.com/thread/xK7Fhx5bpgG7oNv0d)  
线程标题：AI Agent与RAG融合学习路线  
整理时间：2026-09-03

## 线程核心结论

当前目标不是系统背语法或从算法底层开始，而是以一个可演示、可写进简历、可用于面试表达的真实项目来学习 AI 应用工程。

已选择的主攻方向：

- 方向 2：AI Agent 与自动化工作流
- 方向 3：RAG 与知识库工程

推荐主线项目：

- 项目名：企业数据智能体
- 核心思路：把 MySQL 结构化业务数据、非结构化文档知识库和 Agent 工作流合在一起
- 最终效果：用户用自然语言提出业务问题，系统能自动判断查数据库、查文档、联网检索或调用工具，并输出可追溯的答案或报告

该路线的面试价值在于：普通 RAG Demo 往往只做 PDF 问答，而这里会接入真实 MySQL 数据，形成“结构化数据 + 非结构化文档”的混合 RAG，再叠加 Agent 多步骤任务执行，项目真实性更强。

## 建议技术栈

后端与 AI 编排：

- Python
- FastAPI
- LangChain
- LangGraph
- OpenAI 或现有大模型 API

数据与检索：

- MySQL：承载已有业务数据
- Chroma：早期本地向量库，便于快速验证
- Milvus 或 Qdrant：后续可作为生产化升级选项
- Embedding 模型：用于文档向量化
- Rerank 模型：用于提升检索结果质量

部署与工程化：

- Docker
- Nginx
- HTTPS
- 两台服务器分工部署
- 基础鉴权、限流、日志和错误处理

前端：

- 对话窗口
- 对话历史
- 文档上传与管理
- 数据库查询记录
- 答案溯源展示
- 报告导出

## 后续构建流程

### 阶段 0：需求与数据摸底

目标：确认项目边界，避免后续做成泛泛的 Demo。

需要确认：

- MySQL 中具体是什么业务数据，例如订单、用户、商品、日志、内容、招聘或财务数据
- 有哪些核心表、字段和表关联
- 哪些数据可以接入 AI 服务，哪些字段需要脱敏
- 是否允许 AI 服务直接连线上 MySQL，还是需要只读副本
- 每周可投入时间
- 最终偏求职展示、副业接单，还是自用工具

验收标准：

- 形成一份数据源说明
- 选出 3-5 个真实业务问题作为测试用例
- 明确第一版只做哪些功能

### 阶段 1：环境底座与 MySQL 只读接入

目标：先让 AI 能安全读取真实业务数据。

执行步骤：

- 服务器 A 保持 MySQL 原样
- 为 AI 服务创建 MySQL 只读账号
- 限制只允许 `SELECT`
- 配置查询超时和最大返回行数
- 服务器 B 部署 Python、FastAPI、Docker 环境
- 后端建立数据库连接和健康检查接口

验收标准：

- AI 服务能连通 MySQL
- 后端能读取表结构
- 不允许执行写入、更新、删除类 SQL
- 有基础日志记录每次查询

### 阶段 2：Text-to-SQL 最小可用版本

目标：实现“自然语言查数据库”的第一个可演示成果。

执行步骤：

- 使用 LangChain SQLDatabase 或同类工具读取表结构
- 将用户问题转换为 SQL
- 执行 SQL 并返回结果
- 用大模型把查询结果整理成自然语言
- 对生成 SQL 做安全检查
- 保存问题、SQL、执行结果和耗时

验收标准：

- 能回答 3-5 个真实业务问题
- 生成 SQL 可审计
- 错误问题能给出清晰失败原因
- 项目具备第一段简历描述素材

可写简历点：

- 实现基于大模型的 Text-to-SQL 能力，对接 MySQL 业务库，支持自然语言查询、多表查询、安全审计和只读限制。

### 阶段 3：非结构化文档 RAG

目标：把知识来源从数据库扩展到文档。

执行步骤：

- 支持上传或导入 PDF、Word、Markdown、TXT、网页内容
- 完成文档解析、清洗、分块、向量化
- 将向量写入 Chroma
- 实现相似度检索
- 增加 Rerank 提升召回质量
- 回答时展示答案来源片段

验收标准：

- 能基于文档回答问题
- 答案能展示来源
- 能解释分块策略、召回数量、Rerank 的作用

可写简历点：

- 搭建文档 RAG 流程，覆盖文档解析、Chunking、Embedding、向量检索、Rerank 和答案溯源。

### 阶段 4：混合 RAG

目标：把 MySQL 查询和文档检索融合成统一问答系统。

执行步骤：

- 建立查询路由：判断问题应该查数据库、查文档，还是两者都查
- 将 Text-to-SQL 封装成结构化检索工具
- 将文档 RAG 封装成非结构化检索工具
- 设计结果融合层，把数据库结果和文档证据合并回答
- 对外提供统一问答 API

验收标准：

- 同一个接口能处理数据库问题和文档问题
- 涉及多源信息时能融合输出
- 答案能说明使用了哪些数据源

可写简历点：

- 设计并实现结构化数据与非结构化文档的混合 RAG 架构，包含意图路由、多源检索融合和答案溯源。

### 阶段 5：Agent 智能体工作流

目标：从“问答系统”升级为“能自动拆解任务并调用工具的系统”。

执行步骤：

- 使用 LangGraph 搭建 Agent 状态流
- 实现 ReAct 思路：思考、行动、观察、继续推理
- 将数据库查询、文档检索、联网搜索、计算器、文件读写封装为工具
- 构建第一个固定工作流：数据分析报告生成
- 可选增加多 Agent 协作：规划、执行、校验分工

验收标准：

- 输入一个业务目标后，Agent 能拆解步骤
- Agent 能按需调用多个工具
- 能生成 Markdown 分析报告
- 工具调用过程可追踪

可写简历点：

- 基于 LangGraph 实现业务智能体，封装数据查询、文档检索、网络搜索等工具链，支持多步骤任务自动执行与工作流编排。

### 阶段 6：前端产品化

目标：把后端能力包装成完整可演示产品。

执行步骤：

- 做对话式入口
- 做文档上传和知识库管理
- 做数据库查询记录
- 展示 SQL、工具调用链和引用来源
- 支持报告预览与 Markdown 导出
- 增加基础登录或访问控制

验收标准：

- 面试或演示时可以直接打开网页展示
- 普通用户无需懂数据库即可使用
- 关键中间过程可见，便于解释技术深度

### 阶段 7：部署、安全与可观测性

目标：把 Demo 提升到接近生产项目的水平。

执行步骤：

- Docker 化后端服务
- Nginx 反向代理
- HTTPS
- 两台服务器分离：数据节点与 AI 服务节点
- API 鉴权
- 请求限流
- 错误日志
- 查询日志
- 健康检查

验收标准：

- 服务可公网访问
- 异常有日志可查
- 数据库只读访问
- 不暴露敏感配置

### 阶段 8：简历与面试材料整理

目标：把项目转化成可表达的经验。

需要整理：

- 项目架构图
- 数据流图
- Agent 工具调用流程图
- 核心接口说明
- 3-5 个真实演示问题
- 关键技术难点与解决方案
- 可量化指标

建议量化指标：

- 支持多少张业务表
- 支持多少份文档
- 常见问题准确率
- 报告生成耗时
- 相比人工查询节省多少时间

## 当前完成情况

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 明确学习方向 | 已完成 | 已选择 Agent 与 RAG 两个方向 |
| 明确学习方式 | 已完成 | 项目驱动，不以背语法为主 |
| 明确主线项目 | 已完成 | 企业数据智能体 |
| 初步技术路线 | 已完成 | Python、FastAPI、LangChain、LangGraph、MySQL、向量库 |
| MySQL 数据类型确认 | 未完成 | 需要确认具体业务表和数据内容 |
| 服务器分工确认 | 未完成 | 需要确认两台服务器的用途、配置、网络和访问方式 |
| 数据安全策略 | 未完成 | 需要确认只读账号、脱敏字段、查询限制 |
| Text-to-SQL Demo | 未开始 | 阶段 2 的第一版工程成果 |
| 文档 RAG | 未开始 | 阶段 3 的工程成果 |
| 混合 RAG | 未开始 | 阶段 4 的工程成果 |
| Agent 工作流 | 未开始 | 阶段 5 的工程成果 |
| 前端产品化 | 未开始 | 阶段 6 的工程成果 |
| 部署上线 | 未开始 | 阶段 7 的工程成果 |
| 简历与面试材料 | 未开始 | 阶段 8 的最终包装 |

## 下一步优先事项

优先做阶段 0 和阶段 1，不要直接上 Agent。

下一步清单：

- 导出或查看 MySQL 表结构
- 选 3-5 个真实业务问题
- 建 MySQL 只读账号
- 确认 AI 服务部署在哪台服务器
- 确认模型 API 调用方式
- 搭建 FastAPI 最小服务
- 跑通第一个 Text-to-SQL 查询

## 风险与注意事项

- 不要让大模型生成的 SQL 直接拥有写权限
- 不要把敏感业务字段直接送给模型
- 不要一开始追求多 Agent，先把单工具、单流程跑通
- 不要只做文档问答，真实 MySQL 数据才是这个项目的差异化
- 每完成一个阶段都要沉淀截图、接口示例和技术说明，后续面试会直接用到

## 数据地图

第一步的数据地图和 MVP 问题集见 [AI_AGENT_RAG_DATA_AUDIT.md](./AI_AGENT_RAG_DATA_AUDIT.md)。  
当前仓库已确认的数据源是 PostgreSQL 的 `products_key` 和 `seo_key`，不是 MySQL。
